================================================================================
ISSUES
================================================================================
* LEXING

 - approfondire il problema del ricronoscimento dei numeri float vs. integer
   (conditional lexing?)

 - introduzione del token 'variable'

 - fare in modo che gli argomenti di Belief/Goal/Action vengano
   "lexati" come 'variable' e non come 'string'
   (conditional lexing?)

* PARSING
 - start symbol: necessario prevederlo nella specifica della
   grammatica?

 - e` possibile definire la grammatica in modo tale da evitare che
   sequenze del tipo [1,] siano sintatticamente corrette?
   - E' possibile, bisogna solo decidere quale dei due approcci seguire

 - IntentionList: il terminale che fa la 'ricorsione' va per primo o
   no? C`e` differenza?

================================================================================
ROADMAP
================================================================================

 - risoluzione issues lexer
 - risoluzione issues parser
 - scrivere logica per error handling del lexer
 - scrivere logica per error handling del parser
 - definizione classi per rappresentazione AST
 - definizione meccanismo per visita dell'AST e la generazione del codice


================================================================================
FIXED ISSUES
================================================================================
FIXED! - introduzione del token 'not'
FIXED! - prevedere il terminale 'NOT' per il corpo della lambda expr
FIXED! - e` possibile modificare la grammatica per fare in modo che
         l'ultimo LambdaTest debba avere una parentesi tonda?



 


